package AutomatedInventoryManagementSystem;

//includes the java swing gui

public class MainMenu {
	
	// --- Three Horizontal Dots ---
	// add action listener if the button has been pressed do:
	// show Back -> 
	// show Logout -> logout();
	// show Exit -> exits the application
	// show Create Account -> when pressed do createAccountMenu();
	
	// --- Inventory Menu --- 
	// add action listener if the button has been pressed do:
	// openInventoryMenu();
	
	// --- Order Menu --- 
	// add action listener if the button has been pressed do:
	// openOrderMenu();
	
	// --- Sales Report --- 
	// add action listener if the button has been pressed do:
	// openSalesReport();
}
